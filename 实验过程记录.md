# 实验过程记录：Linux 内核阶段 1（自定义内核→VM→printk→syscall）

> 规则：命令全明文、输出全贴、每改源码/重启前打快照、一次破坏一个点。
> 时间：Windows 本机真实时间。

## 2026-08-13 ｜ 环境探测（宿主侧，只读）

**问题**：VM 已关机、SSH config 无 playground 条目、需确认网络/磁盘/工具链。

**执行摘要**：
- vmrun 存在于 `C:\Program Files (x86)\VMware\VMware Workstation\vmrun.exe`；`vmrun list` = 0（全关机）
- playground.vmx 实测：memsize=3072(3G)、numvcpus=2、firmware=efi、磁盘64G、eth0=NAT e1000、eth1=VMnet2 仍在
- guestInfo 显示内核已更新到 6.8.0-136-generic
- D 盘剩 43G；C 盘剩 56G；无 QEMU
- SSH：~/.ssh/config 只有 `Host vm`；id_ed25519 在，playground 需 `ssh pc@192.168.169.66` 待开机验证免密
- 网络三层：
  - 宿主：WLAN 192.168.31.77/24；直连 baidu 200 / cdn.kernel.org ✗ / github ✗
  - 本机代理：mixed 0.0.0.0:10080、socks 0.0.0.0:10081 监听（PID 17636）；系统代理 127.0.0.1:10080；走代理 kernel.org 200 ✓
  - WSL：Ubuntu-24.04 WSL2 mirrored，代理 env 已配，kernel.org 走代理 200 ✓
  - TUNA 镜像：`mirrors.tuna.tsinghua.edu.cn/kernel/` 直连 200 ✓；有 6.8.12 / 6.12.103 / 6.19.14
- 工具链：git 2.51.0 有；gh 未装；git 全局身份未配；GitHub 无已存凭据

**结论**：
- 版本选 linux-6.8.12（同代对齐运行内核，配置零漂移）
- 下载走 TUNA 直连；内存已 3G 不用提；磁盘预算放宽到 ~50G
- GitHub 推送需先解决身份/凭据（gh 未装）

---

## 2026-08-13 ｜ VM 内探测（任务#1）

- uname=6.8.0-136-generic；内存 3G+4G swap；nproc=2
- 磁盘：/=19G 可用、/boot=1.6G、LVM VFree=28.5G（可应急扩容）
- /proc/config.gz 为空（IKCONFIG_PROC 未开）→ **配置改从 /boot/config-6.8.0-136-generic 打底**
- 依赖缺 libelf-dev/flex/bison/libncurses-dev（已装 bc/build-essential/cpio/kmod/libssl-dev）
- **SecureBoot disabled**（自制无签名内核可引导）
- 网络：TUNA / archive.ubuntu / cdn.kernel.org 直连全部 200，免代理
- 免密 sudo 有效（sudo -n whoami=root）；wget/curl/sha256sum 齐全
- SSH 免密直连可用（ssh pc@192.168.169.66，BatchMode OK）

## 2026-08-13 ｜ 任务#2 下载+解压源码

- TUNA 直连下载 `linux-6.8.12.tar.xz` = 142,594,556 字节（12.5s），`sha256sums.asc` 校验 **OK**
- 解压 37.7s → `~/kernel-lab/linux-6.8.12` = **1.6G**
- **apt 装依赖失败**：`/etc/apt/sources.list.d/ubuntu.sources` 指向 `http://mirrors.aliyun.com/ubuntu/`（HTTP 明文），阿里云返回 403 / "no longer signed"（noble-security/backports 全挂）
  - 问题：阿里云 HTTP 镜像拒绝请求
  - 候选修复：TUNA https（已实测 200）、官方 archive.ubuntu.com http（待验）
  - 待用户放行改源（全局配置变更）

## 2026-08-13 ｜ apt 源修复（任务#2 收尾）

- 阿里云 http 403 → 试 https 仍 403（noble-security/backports 和 pool .deb 全拒）
- 按"失败2次停猜"协议，切换到 **TUNA 清华源**（VM 直连实测 200）：
  `sudo sed -i.bak.tuna "s|https://mirrors.aliyun.com/ubuntu/|https://mirrors.tuna.tsinghua.edu.cn/ubuntu/|g" /etc/apt/sources.list.d/ubuntu.sources`
- apt update 成功（39.2MB/8s），依赖装齐：**bison flex libelf-dev libncurses-dev** ✓
- 教训：中国区镜像 http 常被拒，403 排查要带"换源"预案；sed -i.bak 自动留备份

## 2026-08-13 ｜ 任务#3 配置+编译启动

- 配置：`cp /boot/config-6.8.0-136-generic .config` + scripts/config **关 DEBUG_INFO 全家**（DWARF4/5/REDUCED/SPLIT/BTF）+ 清 LOCALVERSION + 关 SYSTEM_TRUSTED_KEYS/REVOCATION_KEYS
- olddefconfig 完成；**坑**：Ubuntu config 的 CONFIG_DEBUG_INFO_DWARF5=y 会把 DEBUG_INFO 重新拽回 y，必须连 DWARF 选择项一起关，否则 build 膨胀到 15G+ 磁盘扛不住
- `make -j2` 后台启动（nohup → `~/kernel-lab/build.log`），预计 25-45 分钟
- 补丁已预写 `patches/`：01_printk / 02_syscall_64.tbl / 03_hello.c / 04_syscalls.h / 05_Makefile / 06_hello_test.c

---

## 待办任务跟踪

- [x] #1 开机探测 playground 实况
- [ ] #2 下载内核源码(TUNA)并进 VM
- [ ] #3 配置+编译内核
- [ ] #4 安装内核+引导+重启验证
- [ ] #5 自定义 printk 验证
- [ ] #6 自定义 syscall 验证
- [ ] #7 验收全项
- [ ] #8 清理与收尾
- [ ] #9 方案2份+过程记录+GitHub 推送
