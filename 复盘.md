# 复盘：Linux 内核阶段 1（自定义内核编译→安装→printk）

- 日期：2026-08-14
- 环境：playground VM（Ubuntu 24.04.4，3G/2vCPU，内核源 6.8.12 主线 stable）
- 结果：**自定义内核 6.8.12 编译→安装→启动✅，自定义 printk 验证✅；自定义 syscall 取消**

---

## 一、完成情况

| 里程碑 | 状态 |
|---|---|
| 源码获取（TUNA 直连 + sha256 校验） | ✅ linux-6.8.12.tar.xz |
| 配置（/boot/config 打底 + 关 DEBUG_INFO 全家） | ✅ |
| 全量编译 | ✅ vmlinux 64.6MB + bzImage 13MB + **6,456 个 .ko** |
| 安装（modules_install + initramfs + grub） | ✅ |
| 重启验证 | ✅ uname -r = 6.8.12，网络正常 |
| 自定义 printk | ✅ dmesg `[0.000000] MY-KERNEL: 我自己编译的内核启动成功` |
| 自定义 syscall 452 | ❌ 已取消（见问题#8） |
| 验收 7 项 | ✅ 全过 |

---

## 二、时间预算复盘（最大教训）

| 项 | 计划 | 实际 | 根因 |
|---|---|---|---|
| 全量编译 | 25-45 分钟 | **5+ 小时** | Ubuntu config 把整棵驱动树编成 **6,456 个模块** |

- 日志总编译单元约 1.4 万+；模块后处理（.mod.o 元数据 + .ko 链接）占了大头
- 加一个 syscall 会牵动 **~23,848 个 .o 重编**（unistd_64.h 近乎全树传递依赖）→ 等价全量重编
- **结论**：发行版式全量构建 vs 学习用裁剪构建是两码事。**后续阶段必须用裁剪 config**（`make defconfig` + 只开目标子系统），单子系统编译省 10 倍时间。

---

## 三、问题 → 原因 → 解决（8 条）

1. **apt 阿里云镜像 403**（含 https 与 pool .deb）
   → 按"失败2次停猜"协议换 **TUNA 清华源** ✓
2. **/proc/config.gz 为空**（CONFIG_IKCONFIG_PROC 未开）
   → 改用 `/boot/config-6.8.0-136-generic` 打底 ✓
3. **DEBUG_INFO 关不掉**（Ubuntu config 的 DWARF5 选择项把 DEBUG_INFO 拽回 y）
   → 连 DEBUG_INFO 全家（DWARF4/5/REDUCED/SPLIT/BTF）一起 `--disable` ✓（否则 build 膨胀 15G+）
4. **SSH 周期性断连**（≥3 次，宿主网络抖动/睡眠）
   → **nohup 保活完美工作**（make PID 全程未变），编译零中断 ✓
5. **C 字符串被真实换行截断**（JSON→bash→ssh→heredoc→python 多层转义，`\n` 被逐层吃成真换行）
   → 改用 **chr() 零转义**（chr(9)/chr(10)/chr(92)）绕开所有转义层 ✓
6. **syscall 452 冲突 + 表未排序报错**
   → 原以为 6.8 末号 451，**实际 461，452 已被 fchmodat2 占用** → 改 462 ✓
   → 铁律：**syscall 号永远先 `grep` 表末号再定**
7. **VM 时钟跳变**（21:09/23:58/00:00 乱跳，宿主睡眠导致）
   → 用 `ps etimes` 量 make 真实时长，墙钟只作参考 ✓
8. **自定义 syscall 取消**
   → 加一个 syscall = unistd_64.h 重生成 = ~24k 对象全量重编（1.5-3h+）
   → 决定取消，运行内核不受影响，撤销 4 处改动
   → **这正实锤了"syscall 号是永久 ABI、内核排斥新 syscall、改走 io_uring/eBPF"的设计原因**

---

## 四、方法论验证（哪些设计经住了考验）

- ✅ **nohup 防断连**：3+ 次 SSH 断连编译零中断——本课最重要的保命设计
- ✅ **回滚票**：GRUB 保留旧内核 6.8.0-136/137，SecureBoot 提前确认 disabled
- ✅ **每课验收**：uname=6.8.12、printk 第一条日志、网络恢复——全部可测通过
- ✅ **真实环境**：全部实测，零编造
- ✅ **实时流式打印**：60s 窗口 tail -f 满足全程可见

---

## 五、可复用经验（下阶段直接套用）

1. **选内核版本**：与运行内核同 major.minor 的上游 stable，配置零漂移
2. **配置打底**：`cp /boot/config-$(uname -r) .config`，再关 DEBUG_INFO 全家
3. **编译环境**：nohup + 后台 + 快照，断连不慌
4. **源码下载**：TUNA 直连 + sha256 校验；中国区镜像 403 直接换源
5. **多层转义操作**：一律用文件脚本 + chr() 零转义
6. **后续阶段**：改用裁剪 config 单子系统编译，省 10 倍时间

---

## 六、下一步

- 阶段 2 核心机制（进程/调度/同步/中断/时间）——用裁剪 config 重新搭环境
- 源码 ~/kernel-lab 已删（本课收尾），重学时 TUNA 重新下载 + 全量重编一次
- 已安装内核 6.8.12 保留运行，可作后续基线
